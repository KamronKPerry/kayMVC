@model IEnumerable<KayMVC.DATA.Responsibility>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EntryID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Responsibility1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ResumeJobEntry.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EntryID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Responsibility1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResumeJobEntry.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ResponsibilityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ResponsibilityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ResponsibilityID })
        </td>
    </tr>
}

</table>
