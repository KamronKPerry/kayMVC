@model IEnumerable<KayMVC.DATA.WritingSample>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SampleTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SampleContext)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SampleAdditionalContext)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GraphicsSample.ImageName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SampleTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SampleContext)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SampleAdditionalContext)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GraphicsSample.ImageName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SampleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SampleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SampleID })
        </td>
    </tr>
}

</table>
